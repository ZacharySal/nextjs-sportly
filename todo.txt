1) Implement Game Overview Page
2) Add key for scoreboard 

news.articles.slice(0, 10).map((article: any, index: number) => (
            <>
              <ArticleCard
                key={article.dataSourceIdentifier}
                article={article}
              />
              {index + 1 != 10 && (
                <Divider className="w-full color-[#edeef0] my-[0.5rem]" />
              )}
            </>
          ))}